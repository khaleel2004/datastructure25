#include<stdio.h>
void main()
{
	int i,a,n;
	printf("Enter the number :");
	scanf("%d",&n);
	a=fact(n);
	printf("%d",a);
	getch();
}
int fact(int n)
{
	int f;
	if (n==1)
	return 1;
	else 
	{
		return(n*fact(n-1));
	}
}
